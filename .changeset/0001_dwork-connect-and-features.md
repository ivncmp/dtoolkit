---
"@dtoolkit/dwork": minor
---

Add `dwork connect` command for configuring AI coding clients (Claude, Codex, Gemini, OpenCode) against a running dwork server. Also: stable 8-char task IDs persisted in BACKLOG.md, atomic task + detail doc creation in a single call, and dashboard version from /health.
